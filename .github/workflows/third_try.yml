name: test

on:
  push:
    branches: [ "test_branch" ]
  pull_request:
    types: [opened, reopened, edited]
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: $HOME/tests/test.py
